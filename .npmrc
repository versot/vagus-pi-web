# Enable pnpm strict workspace package resolution.
strict-peer-dependencies=false
auto-install-peers=true
# Write exact versions for new dependencies (no ^ ranges) — upgrades must
# be explicit, never implicit.
save-exact=true
