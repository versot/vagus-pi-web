pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      run: pnpm exec oxlint {staged_files}
commit-msg:
  commands:
    conventional:
      run: node scripts/check-commit-msg.mjs "{1}"
